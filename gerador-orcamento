<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gerador de Orçamento Contábil</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Inter:wght@400;500;600&display=swap">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js"></script>
    <style>
        :root {
            --bg-dark: #1a1a1a;
            --bg-input: #2a2a2a;
            --ouro: #D4AF37;
            --rose: #C9A9A3;
            --texto: #F5F5F5;
            --texto-segundo: #d0d0d0;
            --border: #3a3a3a;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Inter', sans-serif; background-color: var(--bg-dark); color: var(--texto); line-height: 1.6; }
        .container { display: flex; height: 100vh; }
        .formulario { flex: 0 0 40%; overflow-y: auto; padding: 30px; border-right: 1px solid var(--border); background-color: var(--bg-dark); }
        .preview { flex: 1; overflow-y: auto; padding: 20px; background-color: #0f0f0f; }
        h1 { font-family: 'Playfair Display', serif; font-size: 24px; margin-bottom: 25px; color: var(--ouro); border-bottom: 2px solid var(--rose); padding-bottom: 15px; }
        .secao { margin-bottom: 10px; background-color: var(--bg-input); border-radius: 8px; border-left: 3px solid var(--ouro); overflow: hidden; }
        .secao-titulo { font-size: 14px; font-weight: 600; color: var(--ouro); padding: 15px 20px; text-transform: uppercase; cursor: pointer; display: flex; justify-content: space-between; align-items: center; user-select: none; transition: background-color 0.2s; }
        .secao-titulo:hover { background-color: #3a3a3a; }
        .secao-titulo::after { content: '▼'; font-size: 12px; transition: transform 0.3s; }
        .secao-titulo.ativo::after { transform: rotate(180deg); }
        .secao-conteudo { max-height: 0; overflow: hidden; transition: max-height 0.3s ease-out; padding: 0 20px; }
        .secao-conteudo.ativo { max-height: 2000px; padding: 20px; }
        .campo-grupo { margin-bottom: 15px; }
        label { display: block; font-size: 12px; color: var(--texto-segundo); margin-bottom: 5px; font-weight: 500; }
        input, select, textarea { width: 100%; padding: 10px; background-color: var(--border); border: 1px solid #4a4a4a; border-radius: 4px; color: var(--texto); font-family: 'Inter', sans-serif; font-size: 13px; }
        input:focus, select:focus, textarea:focus { outline: none; border-color: var(--ouro); background-color: #3a3a3a; }
        textarea { resize: vertical; min-height: 80px; }
        .checkbox-label { display: flex; align-items: center; font-size: 13px; margin-bottom: 12px; cursor: pointer; gap: 8px; width: fit-content; }
        input[type="checkbox"] { width: 16px; height: 16px; cursor: pointer; accent-color: var(--ouro); flex-shrink: 0; margin: 0; }
        .botoes { display: flex; gap: 10px; margin-top: 30px; }
        button { flex: 1; padding: 12px; background-color: var(--ouro); color: #1a1a1a; border: none; border-radius: 4px; font-weight: 600; cursor: pointer; font-size: 14px; }
        button:hover { transform: translateY(-2px); opacity: 0.9; }
        button.secundario { background-color: var(--rose); }
        
        .pdf-container #logoPreview { width: 200px; height: 140px; background-size: contain; background-position: center; background-repeat: no-repeat; flex-shrink: 0; }
        #logoPreview { background-size: contain; background-position: center; background-repeat: no-repeat; }
        .pdf-container { width: 100%; max-width: 850px; background-color: white; background-size: 100% auto; background-position: top center; background-repeat: no-repeat; background-attachment: scroll; color: #000; margin: 0 auto; padding: 40px; font-family: 'Inter', sans-serif; line-height: 1.8; min-height: auto; position: relative; }
        .pdf-container::before { content: ''; position: absolute; top: 0; left: 0; right: 0; bottom: 0; background: rgba(255, 255, 255, 0.3); pointer-events: none; z-index: 0; }
        .pdf-container > * { position: relative; z-index: 1; }
        .pdf-header { display: flex; justify-content: space-between; align-items: center; gap: 20px; margin-bottom: 30px; border-bottom: 2px solid #D4AF37; padding-bottom: 20px; }
        .pdf-header > div:first-child { flex: 1; }
        .pdf-header h1 { font-family: 'Playfair Display', serif; font-size: 20px; color: #1a1a1a; margin: 0; border: none; padding: 0; }
        .pdf-header p { font-size: 12px; color: #666; margin: 5px 0 0 0; }
        .pdf-data { display: grid; grid-template-columns: repeat(2, 1fr); gap: 15px; margin-bottom: 30px; background-color: #f9f9f9; padding: 15px; border-radius: 4px; }
        .pdf-data-item { display: flex; flex-direction: column; }
        .pdf-data-label { font-size: 11px; color: #999; font-weight: 600; text-transform: uppercase; margin-bottom: 3px; }
        .pdf-data-valor { font-size: 13px; color: #1a1a1a; font-weight: 500; }
        .pdf-secao { margin-bottom: 18px; padding-bottom: 18px; border-bottom: 1px solid #e0e0e0; }
        .pdf-secao:nth-of-type(1), .pdf-secao:nth-of-type(2), .pdf-secao:nth-of-type(3) { border-bottom: 2px solid #D4AF37; padding-bottom: 25px; margin-bottom: 25px; }
        .pdf-secao-titulo { font-size: 13px; font-weight: 700; color: #1a1a1a; margin-bottom: 12px; text-transform: uppercase; border-left: 3px solid #D4AF37; padding-left: 10px; }
        .pdf-titulo-cliente { font-size: 14px; font-weight: 700; color: #1a1a1a; margin-bottom: 15px; text-transform: uppercase; padding-bottom: 10px; }
        .pdf-secao-conteudo { font-size: 12px; color: #333; line-height: 1.8; padding: 0 0 10px 0; }
        .pdf-secao-conteudo p { margin-bottom: 10px; }
        .planos-tabela { width: 100%; border-collapse: collapse; margin-bottom: 12px; font-size: 12px; }
        .planos-tabela th { padding: 8px; text-align: left; font-weight: 600; color: #1a1a1a; background-color: #f0f0f0; border-bottom: 2px solid #D4AF37; }
        .planos-tabela td { padding: 8px; border-bottom: 1px solid #eee; color: #333; }
        .servicos-list { list-style: none; padding-left: 0; font-size: 12px; color: #333; }
        .servicos-list li { padding-left: 20px; margin-bottom: 5px; position: relative; }
        .servicos-list li:before { content: '✓'; position: absolute; left: 0; color: #D4AF37; font-weight: bold; }
        .pdf-footer { margin-top: 40px; border-top: 2px solid #D4AF37; padding-top: 15px; text-align: center; font-size: 11px; color: #666; }
        .pdf-footer p { margin: 3px 0; }
        .info-box { background-color: #f5f5f5; border-left: 3px solid #D4AF37; padding: 12px; margin-bottom: 15px; font-size: 12px; color: #333; border-radius: 2px; }
        .info-box p { margin-bottom: 10px; line-height: 1.6; }
        .info-box p:last-child { margin-bottom: 0; }
        .info-box strong { color: #1a1a1a; font-weight: 600; }
        .proximos-passos { font-size: 12px; color: #333; }
        .proximos-passos ol { margin-left: 20px; margin-bottom: 10px; }
        .proximos-passos li { margin-bottom: 8px; }
        .incluido { color: #2d5016; }
    </style>
</head>
<body>
    <div class="container">
        <div class="formulario">
            <h1>Gerador de Orçamento</h1>
            <button type="button" onclick="limparDados()" style="width: 100%; padding: 8px; margin-bottom: 20px; background-color: #ff6b6b; color: white; border: none; border-radius: 4px; cursor: pointer; font-size: 12px; font-weight: 600;">🗑️ Limpar Dados Salvos</button>

            <div class="secao">
                <div class="secao-titulo ativo">| CLIENTE</div>
                <div class="secao-conteudo ativo">
                    <div class="campo-grupo"><label>Nome/Razão Social</label><input type="text" id="nomeCliente" placeholder="Ex: João Silva"></div>
                    <div class="campo-grupo"><label>Empresa</label><input type="text" id="nomeEmpresa" placeholder="Ex: Silva Consultoria"></div>
                    <div class="campo-grupo"><label>Atividade Principal</label><input type="text" id="atividade" placeholder="Ex: Consultoria em TI"></div>
                    <div class="campo-grupo">
                        <label>CNAE (até 5)</label>
                        <div style="display: flex; gap: 10px; margin-bottom: 10px;">
                            <input type="text" id="cnae1" placeholder="Ex: 6202-3/00 - Consultoria em tecnologia" style="flex: 1;">
                            <button type="button" onclick="window.open('https://concla.ibge.gov.br/busca-online-cnae.html', '_blank')" style="padding: 10px 15px; background-color: var(--rose); color: #1a1a1a; border: none; border-radius: 4px; cursor: pointer; font-weight: 600; font-size: 13px; flex-shrink: 0;">🔗 CNAE</button>
                        </div>
                        <div style="display: flex; gap: 10px; margin-bottom: 10px;">
                            <input type="text" id="cnae2" placeholder="CNAE 2 (opcional)" style="flex: 1;">
                        </div>
                        <div style="display: flex; gap: 10px; margin-bottom: 10px;">
                            <input type="text" id="cnae3" placeholder="CNAE 3 (opcional)" style="flex: 1;">
                        </div>
                        <div style="display: flex; gap: 10px; margin-bottom: 10px;">
                            <input type="text" id="cnae4" placeholder="CNAE 4 (opcional)" style="flex: 1;">
                        </div>
                        <div style="display: flex; gap: 10px;">
                            <input type="text" id="cnae5" placeholder="CNAE 5 (opcional)" style="flex: 1;">
                        </div>
                    </div>
                    <div class="campo-grupo"><label>Data da Proposta</label><input type="date" id="dataProposta"></div>
                    <div class="campo-grupo"><label>Faturamento Anual (R$)</label><input type="number" id="faturamento" placeholder="Ex: 150000" step="1000"></div>
                </div>
            </div>

            <div class="secao">
                <div class="secao-titulo">Dados do Seu Escritório</div>
                <div class="secao-conteudo">
                    <div class="campo-grupo"><label>Nome do Escritório</label><input type="text" id="nomeEscritorio" placeholder="Ex: Jaqueline Ferreira Contadora"></div>
                    <div class="campo-grupo"><label>Responsável</label><input type="text" id="responsavel" placeholder="Ex: Jaqueline Ferreira"></div>
                    <div class="campo-grupo"><label>CRC</label><input type="text" id="crc" placeholder="Ex: 1.234.567"></div>
                    <div class="campo-grupo"><label>Telefone</label><input type="tel" id="telefone" placeholder="Ex: (11) 99999-9999"></div>
                    <div class="campo-grupo"><label>Email</label><input type="email" id="email" placeholder="Ex: contato@escritorio.com.br"></div>
                    <div class="campo-grupo"><label>📄 Papel Timbrado (opcional)</label><input type="file" id="papelTimbrado" accept="image/*"><small style="color: var(--texto-segundo); display: block; margin-top: 5px;">JPG ou PNG — folha inteira</small><div id="papelAviso" style="color: #ff6b6b; font-size: 11px; margin-top: 5px; display: none;"></div></div>
                <div class="campo-grupo"><label>🏷️ Logo (opcional)</label><input type="file" id="logo" accept="image/*"><small style="color: var(--texto-segundo); display: block; margin-top: 5px;">JPG ou PNG — pequena no topo</small></div>
                </div>
            </div>

            <div class="secao">
                <div class="secao-titulo">📝 Introdução</div>
                <div class="secao-conteudo">
                    <div class="campo-grupo"><label>Texto Introdutório</label><textarea id="introducao" placeholder="Prezado [Cliente]..."></textarea></div>
                </div>
            </div>

            <div class="secao">
                <div class="secao-titulo ativo">📊 Regimes Tributários</div>
                <div class="secao-conteudo ativo">
                    <div style="padding: 12px; background-color: #1a1a1a; border-radius: 4px; display: flex; flex-direction: column;">
                        <div style="font-size: 12px; color: var(--ouro); margin-bottom: 15px; font-weight: 600;">Marque quais regimes entrarão na proposta:</div>
                        <label class="checkbox-label"><input type="checkbox" id="usarMEI" checked><span>MEI</span></label>
                        <label class="checkbox-label"><input type="checkbox" id="usarSimples" checked><span>Simples Nacional</span></label>
                        <label class="checkbox-label"><input type="checkbox" id="usarPresumido" checked><span>Presumido</span></label>
                    </div>
                </div>
            </div>

            <div class="secao">
                <div class="secao-titulo">📊 MEI</div>
                <div class="secao-conteudo">
                    <div class="campo-grupo"><label>Descrição/Estratégia</label><textarea id="meiDescricao" placeholder="Ex: Ideal para profissionais autônomos..."></textarea></div>
                    <div style="background-color: #1a1a1a; padding: 15px; border-radius: 4px; margin-bottom: 10px;">
                    <label class="checkbox-label" style="color: var(--ouro); font-weight: 600; font-size: 12px; margin-bottom: 15px;">
                        <input type="checkbox" id="mei1check" checked>
                        <span>PLANO 1 - BÁSICO</span>
                    </label>
                    <div class="campo-grupo"><label>Nome</label><input type="text" id="mei1nome" placeholder="Básico" value="Básico"></div>
                    <div class="campo-grupo"><label>Valor/Mês</label><input type="number" id="mei1valor" step="0.01" placeholder="0.00"></div>
                    <div class="campo-grupo"><label>Descrição do Plano</label><textarea id="mei1desc" placeholder="O que está incluído neste plano..." style="min-height: 60px;"></textarea></div>
                </div>
                <div style="background-color: #1a1a1a; padding: 15px; border-radius: 4px; margin-bottom: 10px;">
                    <label class="checkbox-label" style="color: var(--ouro); font-weight: 600; font-size: 12px; margin-bottom: 15px;">
                        <input type="checkbox" id="mei2check" checked>
                        <span>PLANO 2 - PROFISSIONAL</span>
                    </label>
                    <div class="campo-grupo"><label>Nome</label><input type="text" id="mei2nome" placeholder="Profissional" value="Profissional"></div>
                    <div class="campo-grupo"><label>Valor/Mês</label><input type="number" id="mei2valor" step="0.01" placeholder="0.00"></div>
                    <div class="campo-grupo"><label>Descrição do Plano</label><textarea id="mei2desc" placeholder="O que está incluído neste plano..." style="min-height: 60px;"></textarea></div>
                </div>
                <div style="background-color: #1a1a1a; padding: 15px; border-radius: 4px;">
                    <label class="checkbox-label" style="color: var(--ouro); font-weight: 600; font-size: 12px; margin-bottom: 15px;">
                        <input type="checkbox" id="mei3check" checked>
                        <span>PLANO 3 - PREMIUM</span>
                    </label>
                    <div class="campo-grupo"><label>Nome</label><input type="text" id="mei3nome" placeholder="Premium" value="Premium"></div>
                    <div class="campo-grupo"><label>Valor/Mês</label><input type="number" id="mei3valor" step="0.01" placeholder="0.00"></div>
                    <div class="campo-grupo"><label>Descrição do Plano</label><textarea id="mei3desc" placeholder="O que está incluído neste plano..." style="min-height: 60px;"></textarea></div>
                    </div>
                </div>
            </div>

            <div class="secao">
                <div class="secao-titulo">📊 SIMPLES NACIONAL</div>
                <div class="secao-conteudo">
                <div class="campo-grupo"><label>Descrição/Estratégia</label><textarea id="simplesDescricao" placeholder="Ex: Regime com menor carga tributária..."></textarea></div>
                <div style="background-color: #1a1a1a; padding: 15px; border-radius: 4px; margin-bottom: 10px;">
                    <label class="checkbox-label" style="color: var(--ouro); font-weight: 600; font-size: 12px; margin-bottom: 15px;">
                        <input type="checkbox" id="simples1check" checked>
                        <span>PLANO 1 - BÁSICO</span>
                    </label>
                    <div class="campo-grupo"><label>Nome</label><input type="text" id="simples1nome" placeholder="Básico" value="Básico"></div>
                    <div class="campo-grupo"><label>Valor/Mês</label><input type="number" id="simples1valor" step="0.01" placeholder="0.00"></div>
                    <div class="campo-grupo"><label>Descrição do Plano</label><textarea id="simples1desc" placeholder="O que está incluído neste plano..." style="min-height: 60px;"></textarea></div>
                </div>
                <div style="background-color: #1a1a1a; padding: 15px; border-radius: 4px; margin-bottom: 10px;">
                    <label class="checkbox-label" style="color: var(--ouro); font-weight: 600; font-size: 12px; margin-bottom: 15px;">
                        <input type="checkbox" id="simples2check" checked>
                        <span>PLANO 2 - PROFISSIONAL</span>
                    </label>
                    <div class="campo-grupo"><label>Nome</label><input type="text" id="simples2nome" placeholder="Profissional" value="Profissional"></div>
                    <div class="campo-grupo"><label>Valor/Mês</label><input type="number" id="simples2valor" step="0.01" placeholder="0.00"></div>
                    <div class="campo-grupo"><label>Descrição do Plano</label><textarea id="simples2desc" placeholder="O que está incluído neste plano..." style="min-height: 60px;"></textarea></div>
                </div>
                <div style="background-color: #1a1a1a; padding: 15px; border-radius: 4px;">
                    <label class="checkbox-label" style="color: var(--ouro); font-weight: 600; font-size: 12px; margin-bottom: 15px;">
                        <input type="checkbox" id="simples3check" checked>
                        <span>PLANO 3 - PREMIUM</span>
                    </label>
                    <div class="campo-grupo"><label>Nome</label><input type="text" id="simples3nome" placeholder="Premium" value="Premium"></div>
                    <div class="campo-grupo"><label>Valor/Mês</label><input type="number" id="simples3valor" step="0.01" placeholder="0.00"></div>
                    <div class="campo-grupo"><label>Descrição do Plano</label><textarea id="simples3desc" placeholder="O que está incluído neste plano..." style="min-height: 60px;"></textarea></div>
                    </div>
                </div>
            </div>

            <div class="secao">
                <div class="secao-titulo">📊 PRESUMIDO</div>
                <div class="secao-conteudo">
                <div class="campo-grupo"><label>Descrição/Estratégia</label><textarea id="presumidoDescricao" placeholder="Ex: Para empresas com faturamento maior..."></textarea></div>
                <div style="background-color: #1a1a1a; padding: 15px; border-radius: 4px; margin-bottom: 10px;">
                    <label class="checkbox-label" style="color: var(--ouro); font-weight: 600; font-size: 12px; margin-bottom: 15px;">
                        <input type="checkbox" id="presun1check" checked>
                        <span>PLANO 1 - BÁSICO</span>
                    </label>
                    <div class="campo-grupo"><label>Nome</label><input type="text" id="presun1nome" placeholder="Básico" value="Básico"></div>
                    <div class="campo-grupo"><label>Valor/Mês</label><input type="number" id="presun1valor" step="0.01" placeholder="0.00"></div>
                    <div class="campo-grupo"><label>Descrição do Plano</label><textarea id="presun1desc" placeholder="O que está incluído neste plano..." style="min-height: 60px;"></textarea></div>
                </div>
                <div style="background-color: #1a1a1a; padding: 15px; border-radius: 4px; margin-bottom: 10px;">
                    <label class="checkbox-label" style="color: var(--ouro); font-weight: 600; font-size: 12px; margin-bottom: 15px;">
                        <input type="checkbox" id="presun2check" checked>
                        <span>PLANO 2 - PROFISSIONAL</span>
                    </label>
                    <div class="campo-grupo"><label>Nome</label><input type="text" id="presun2nome" placeholder="Profissional" value="Profissional"></div>
                    <div class="campo-grupo"><label>Valor/Mês</label><input type="number" id="presun2valor" step="0.01" placeholder="0.00"></div>
                    <div class="campo-grupo"><label>Descrição do Plano</label><textarea id="presun2desc" placeholder="O que está incluído neste plano..." style="min-height: 60px;"></textarea></div>
                </div>
                <div style="background-color: #1a1a1a; padding: 15px; border-radius: 4px;">
                    <label class="checkbox-label" style="color: var(--ouro); font-weight: 600; font-size: 12px; margin-bottom: 15px;">
                        <input type="checkbox" id="presun3check" checked>
                        <span>PLANO 3 - PREMIUM</span>
                    </label>
                    <div class="campo-grupo"><label>Nome</label><input type="text" id="presun3nome" placeholder="Premium" value="Premium"></div>
                    <div class="campo-grupo"><label>Valor/Mês</label><input type="number" id="presun3valor" step="0.01" placeholder="0.00"></div>
                    <div class="campo-grupo"><label>Descrição do Plano</label><textarea id="presun3desc" placeholder="O que está incluído neste plano..." style="min-height: 60px;"></textarea></div>
                    </div>
                </div>
            </div>

            <div class="secao">
                <div class="secao-titulo">⭐ Além do Básico</div>
                <div class="secao-conteudo">
                    <div class="campo-grupo"><label>O Que Você Recebe Além do Básico (use Enter OU | para separar itens)</label><textarea id="alem" placeholder="Ex:&#10;Suporte via WhatsApp: descrição... | Reuniões mensais: descrição..."></textarea></div>
                </div>
            </div>

            <div class="secao">
                <div class="secao-titulo">ℹ️ Informações Importantes</div>
                <div class="secao-conteudo">
                    <div class="campo-grupo"><label>Nota 1 (use Enter OU | para separar tópicos)</label><textarea id="nota1" placeholder="Ex:&#10;Abertura do MEI: descrição... | Acompanhamento: descrição..."></textarea></div>
                    <div class="campo-grupo"><label>Nota 2 (use Enter OU | para separar tópicos)</label><textarea id="nota2" placeholder="Ex:&#10;Suporte: descrição... | Orientação: descrição..."></textarea></div>
                </div>
            </div>

            <div class="secao">
                <div class="secao-titulo">📋 Condições</div>
                <div class="secao-conteudo">
                    <div class="campo-grupo"><textarea id="condicoes"></textarea></div>
                </div>
            </div>

            <div class="secao">
                <div class="secao-titulo">🎯 Próximos Passos</div>
                <div class="secao-conteudo">
                    <div class="campo-grupo"><input type="text" id="passo1" placeholder="Passo 1"></div>
                    <div class="campo-grupo"><input type="text" id="passo2" placeholder="Passo 2"></div>
                    <div class="campo-grupo"><input type="text" id="passo3" placeholder="Passo 3"></div>
                    <div class="campo-grupo"><input type="text" id="passo4" placeholder="Passo 4"></div>
                </div>
            </div>

            <div class="botoes">
                <button onclick="gerarPDF()">⬇️ Baixar PDF</button>
                <button class="secundario" onclick="abrirWhatsApp()">📱 WhatsApp</button>
                <button class="secundario" onclick="limpar()">🗑️ Limpar</button>
            </div>
        </div>

        <div class="preview">
            <div id="pdfPreview" class="pdf-container"></div>
        </div>
    </div>

    <script>
        let imagemTimbrado = "";
        let imagemLogo = "";

        // Accordion - abrir/fechar seções
        document.querySelectorAll(".secao-titulo").forEach(titulo => {
            titulo.addEventListener("click", function() {
                const conteudo = this.nextElementSibling;
                const estaAberto = this.classList.contains("ativo");

                this.classList.toggle("ativo");
                conteudo.classList.toggle("ativo");
            });
        });

        document.getElementById("dataProposta").valueAsDate = new Date();

        document.getElementById("papelTimbrado").addEventListener("change", function(e) {
            const file = e.target.files[0];
            const aviso = document.getElementById("papelAviso");
            aviso.style.display = "none";

            if (file) {
                if (file.type.startsWith("image/")) {
                    const reader = new FileReader();
                    reader.onload = function(event) {
                        imagemTimbrado = event.target.result;
                        document.getElementById("pdfPreview").style.backgroundImage = "url(" + imagemTimbrado + ")";
                        atualizar();
                    };
                    reader.readAsDataURL(file);
                } else if (file.type === "application/pdf") {
                    aviso.style.display = "block";
                    aviso.textContent = "⚠️ PDF não é suportado. Use JPG ou PNG";
                }
            }
        });

        document.getElementById("logo").addEventListener("change", function(e) {
            const file = e.target.files[0];
            if (file && file.type.startsWith("image/")) {
                const reader = new FileReader();
                reader.onload = function(event) {
                    imagemLogo = event.target.result;
                    atualizar();
                };
                reader.readAsDataURL(file);
            }
        });

        function atualizar() {
            const data = {
                nomeCliente: document.getElementById("nomeCliente").value || "Seu Cliente",
                nomeEmpresa: document.getElementById("nomeEmpresa").value,
                atividade: document.getElementById("atividade").value,
                cnae1: document.getElementById("cnae1").value,
                cnae2: document.getElementById("cnae2").value,
                cnae3: document.getElementById("cnae3").value,
                cnae4: document.getElementById("cnae4").value,
                cnae5: document.getElementById("cnae5").value,
                dataProposta: document.getElementById("dataProposta").value,
                nomeEscritorio: document.getElementById("nomeEscritorio").value,
                responsavel: document.getElementById("responsavel").value,
                crc: document.getElementById("crc").value,
                telefone: document.getElementById("telefone").value,
                email: document.getElementById("email").value,
                faturamento: document.getElementById("faturamento").value,
                introducao: document.getElementById("introducao").value,
                alem: document.getElementById("alem").value,
                nota1: document.getElementById("nota1").value,
                nota2: document.getElementById("nota2").value,
                condicoes: document.getElementById("condicoes").value,
                passo1: document.getElementById("passo1").value || "Escolher regime",
                passo2: document.getElementById("passo2").value || "Enviar documentação",
                passo3: document.getElementById("passo3").value || "Assinar contrato",
                passo4: document.getElementById("passo4").value || "Iniciar serviços"
            };

            const dataBR = new Date(data.dataProposta).toLocaleDateString("pt-BR", {year: "numeric", month: "long", day: "numeric"});

            let html = '<div class="pdf-header"><div><h1>PROPOSTA DE PRESTAÇÃO DE SERVIÇOS CONTÁBEIS</h1><p>' + data.nomeEmpresa.toUpperCase() + '</p></div><div id="logoPreview"></div></div>';
            html += '<div class="pdf-titulo-cliente">| CLIENTE</div>';
            html += '<div class="pdf-data">';
            html += '<div class="pdf-data-item"><div class="pdf-data-label">Cliente</div><div class="pdf-data-valor">' + data.nomeCliente + '</div></div>';
            html += '<div class="pdf-data-item"><div class="pdf-data-label">Atividade</div><div class="pdf-data-valor">' + data.atividade + '</div></div>';

            // Exibe CNAEs preenchidos
            let cnaesPreenchidos = [];
            if (data.cnae1) cnaesPreenchidos.push(data.cnae1);
            if (data.cnae2) cnaesPreenchidos.push(data.cnae2);
            if (data.cnae3) cnaesPreenchidos.push(data.cnae3);
            if (data.cnae4) cnaesPreenchidos.push(data.cnae4);
            if (data.cnae5) cnaesPreenchidos.push(data.cnae5);

            if (cnaesPreenchidos.length > 0) {
                html += '<div class="pdf-data-item" style="grid-column: 1 / -1;"><div class="pdf-data-label">CNAE(s)</div><div class="pdf-data-valor">' + cnaesPreenchidos.join(' • ') + '</div></div>';
            }

            html += '<div class="pdf-data-item"><div class="pdf-data-label">Data</div><div class="pdf-data-valor">' + dataBR + '</div></div>';
            html += '<div class="pdf-data-item"><div class="pdf-data-label">Faturamento Anual</div><div class="pdf-data-valor">' + (data.faturamento ? 'R$ ' + parseFloat(data.faturamento).toLocaleString('pt-BR', {minimumFractionDigits: 2, maximumFractionDigits: 2}) : '—') + '</div></div>';
            html += '<div class="pdf-data-item"><div class="pdf-data-label">Validade</div><div class="pdf-data-valor">07 dias</div></div>';
            html += '</div>';

            // Seção de Limites de Faturamento
            html += '<div class="pdf-secao" style="background-color: #f9f9f9; padding: 15px; border-left: 3px solid #D4AF37; border-bottom: 1px solid #e0e0e0;"><div class="pdf-secao-titulo" style="margin-bottom: 10px;">📊 LIMITES DE FATURAMENTO POR REGIME</div><div class="pdf-secao-conteudo"><p><strong>MEI:</strong> Até R$ 81.500/ano</p><p><strong>Simples Nacional:</strong> Até R$ 4.800.000/ano</p><p><strong>Lucro Presumido:</strong> Sem limite de faturamento</p></div></div>';

            if (data.introducao) {
                html += '<div class="pdf-secao"><div class="pdf-secao-titulo">INTRODUÇÃO</div><div class="pdf-secao-conteudo"><p>' + data.introducao.replace(/\n/g, '<br>') + '</p></div></div>';
            }

            if (document.getElementById("usarMEI").checked) {
                html += geraPlano("MEI", "mei", "meiDescricao");
            }
            if (document.getElementById("usarSimples").checked) {
                html += geraPlano("SIMPLES NACIONAL", "simples", "simplesDescricao");
            }
            if (document.getElementById("usarPresumido").checked) {
                html += geraPlano("PRESUMIDO", "presun", "presumidoDescricao");
            }

            if (data.alem) {
                html += '<div class="pdf-secao"><div class="pdf-secao-titulo">O QUE VOCÊ RECEBE ALÉM DO BÁSICO</div><div class="pdf-secao-conteudo">' + formatarNota(data.alem) + '</div></div>';
            }

            function formatarNota(texto) {
                if (!texto) return '';
                // Quebra por linhas (Enter/quebra de parágrafo) OU por pipe (|) - trata Windows (\r\n) e Unix (\n)
                let linhas = texto.replace(/\r\n/g, '\n').split(/[\n|]/);
                let html = '';

                linhas.forEach(linha => {
                    linha = linha.trim();
                    if (linha) {
                        // Se tem ":", coloca a primeira parte em negrito
                        if (linha.includes(':')) {
                            let [titulo, conteudo] = linha.split(':', 2);
                            html += '<p><strong>' + titulo.trim() + ':</strong> ' + conteudo.trim() + '</p>';
                        } else {
                            html += '<p>' + linha + '</p>';
                        }
                    }
                });

                return html;
            }

            let notas = '';
            if (data.nota1) notas += '<div class="info-box">' + formatarNota(data.nota1) + '</div>';
            if (data.nota2) notas += '<div class="info-box">' + formatarNota(data.nota2) + '</div>';
            if (notas) {
                html += '<div class="pdf-secao"><div class="pdf-secao-titulo">INFORMAÇÕES IMPORTANTES</div><div class="pdf-secao-conteudo">' + notas + '</div></div>';
            }

            if (data.condicoes) {
                html += '<div class="pdf-secao"><div class="pdf-secao-titulo">CONDIÇÕES CONTRATUAIS</div><div class="pdf-secao-conteudo"><p>' + data.condicoes.replace(/\n/g, '<br>') + '</p></div></div>';
            }

            html += '<div class="pdf-secao"><div class="pdf-secao-titulo">PRÓXIMOS PASSOS</div><div class="proximos-passos"><ol>';
            html += '<li>' + data.passo1 + '</li><li>' + data.passo2 + '</li><li>' + data.passo3 + '</li><li>' + data.passo4 + '</li>';
            html += '</ol></div></div>';

            html += '<div class="pdf-secao"><div class="pdf-secao-titulo">NOSSO COMPROMISSO</div><div class="pdf-secao-conteudo"><p>Atuamos como parceiros estratégicos desde o primeiro dia, garantindo conformidade fiscal, contábil e trabalhista para o crescimento sustentado do seu negócio.</p></div></div>';

            html += '<div class="pdf-footer"><p><strong>' + data.nomeEscritorio + '</strong></p><p>' + data.responsavel + ' — CRC ' + data.crc + '</p><p>' + data.telefone + ' | ' + data.email + '</p></div>';

            document.getElementById("pdfPreview").innerHTML = html;

            if (imagemLogo) {
                document.getElementById("logoPreview").style.backgroundImage = "url(" + imagemLogo + ")";
            }
        }

        function geraPlano(nome, tipo, idDesc) {
            const desc = document.getElementById(idDesc).value;

            let html = '<div class="pdf-secao"><div class="pdf-secao-titulo">' + nome + '</div>';
            if (desc) html += '<div class="pdf-secao-conteudo"><p>' + desc + '</p></div>';

            html += '<table class="planos-tabela"><thead><tr><th>Plano</th><th>Valor/Mês</th></tr></thead><tbody>';

            for (let i = 1; i <= 3; i++) {
                const checkboxID = tipo + i + 'check';
                const nomeID = tipo + i + 'nome';
                const valorID = tipo + i + 'valor';
                const descID = tipo + i + 'desc';
                const isChecked = document.getElementById(checkboxID).checked;
                const nomePlano = document.getElementById(nomeID).value;
                const valorPlano = parseFloat(document.getElementById(valorID).value) || 0;
                const descPlano = document.getElementById(descID).value;

                if (isChecked && (nomePlano || valorPlano > 0)) {
                    html += '<tr><td><strong>' + nomePlano + '</strong></td><td>R$ ' + valorPlano.toFixed(2).replace('.', ',') + '</td></tr>';
                    if (descPlano) {
                        html += '<tr><td colspan="2" style="font-size: 11px; color: #666; padding-top: 0;">' + descPlano + '</td></tr>';
                    }
                }
            }

            html += '</tbody></table></div>';
            return html;
        }

        function gerarPDF() {
            const element = document.getElementById("pdfPreview");
            const opt = {margin: 10, filename: 'proposta.pdf', image: {type: 'jpeg', quality: 0.98}, html2canvas: {scale: 2}, jsPDF: {orientation: 'portrait', unit: 'mm', format: 'a4'}};
            html2pdf().set(opt).from(element).save();
        }

        function abrirWhatsApp() {
            // Faz download do PDF
            gerarPDF();

            // Abre WhatsApp Web com mensagem
            const nomeCliente = document.getElementById("nomeCliente").value || "Cliente";
            const nomeEscritorio = document.getElementById("nomeEscritorio").value || "Escritório Contábil";
            const mensagem = `Olá ${nomeCliente}! 👋\n\nSegue em anexo a proposta de serviços contábeis de ${nomeEscritorio}.\n\nFique à vontade para tirar dúvidas! 📋`;
            const urlWhatsApp = `https://web.whatsapp.com/send?text=${encodeURIComponent(mensagem)}`;
            window.open(urlWhatsApp, '_blank');
        }

        function limpar() {
            if (confirm("Limpar tudo?")) {
                document.querySelectorAll("input, textarea").forEach(el => { el.value = ""; el.checked = false; });
                document.getElementById("dataProposta").valueAsDate = new Date();
                atualizar();
            }
        }

        // Salvamento e carregamento automático
        function salvarDados() {
            const dados = {};
            document.querySelectorAll("input, textarea").forEach(el => {
                if (el.type === "checkbox") {
                    dados[el.id] = el.checked;
                } else if (el.type !== "file") {
                    dados[el.id] = el.value;
                }
            });
            localStorage.setItem("orcamentoData", JSON.stringify(dados));
        }

        function carregarDados() {
            const dados = localStorage.getItem("orcamentoData");
            if (dados) {
                const parsed = JSON.parse(dados);
                Object.keys(parsed).forEach(id => {
                    const el = document.getElementById(id);
                    if (el) {
                        if (el.type === "checkbox") {
                            el.checked = parsed[id];
                        } else {
                            el.value = parsed[id];
                        }
                    }
                });
            }
        }

        function limparDados() {
            if (confirm("Tem certeza que quer limpar todos os dados salvos?")) {
                localStorage.removeItem("orcamentoData");
                document.querySelectorAll("input, textarea").forEach(el => {
                    if (el.type === "checkbox") el.checked = false;
                    else if (el.type !== "file") el.value = "";
                });
                imagemLogo = "";
                imagemTimbrado = "";
                atualizar();
            }
        }

        document.querySelectorAll("input, textarea").forEach(el => {
            el.addEventListener("change", () => { atualizar(); salvarDados(); });
            el.addEventListener("keyup", () => { atualizar(); salvarDados(); });
        });

        carregarDados();
        atualizar();
    </script>
</body>
</html>
